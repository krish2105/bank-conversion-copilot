# Model Card — Bank Conversion Copilot

> Auto-generated by `src/models/train.py` from `artifacts/metrics.json`. Do not edit by hand.

## Overview

| Field | Value |
|---|---|
| Winner | logistic_regression |
| Trained at | 2026-08-12T17:11:54.932358+00:00 |
| sklearn version | 1.8.0 |
| Selection metric | average_precision (PR-AUC) |
| Decision threshold | 0.0350 |

## Intended use

Rank outbound term-deposit telemarketing prospects by expected net value,
for a capacity-constrained call centre deciding who to phone.

## Out-of-scope use

**Not for credit decisions.** Conversion propensity is not a proxy for
credit risk, and this model was never validated for that purpose.

## Test performance

| Model | Validation AP | Validation ROC-AUC |
|---|---|---|
| logistic_regression | 0.1725 | 0.6702 |
| hist_gradient_boosting | 0.1156 | 0.6007 |

Test set (opened once): precision 0.0806, recall 0.6981,
average precision 0.0838, ROC-AUC 0.5891.
Accuracy is reported last, deliberately: 0.4545 versus a
majority-class baseline of 0.9340 —
accuracy alone cannot distinguish this model from predicting "no" for
everyone.

## Business framing

Break-even call probability: 0.0667.
Chosen threshold yields a validation uplift of 2248.00 EUR
versus the naive 0.5 cutoff and 1520.00 EUR
versus calling everyone.

## Leakage controls

- **duration**: Call duration is only known after the call has happened. Using it to decide whether to place the call is impossible in production, and it inflates offline metrics by exposing the outcome (UCI's own documentation flags this as target leakage).

## Drift monitoring (train vs test)

Verdict: **RETRAIN RECOMMENDED**. See `artifacts/drift.json` for per-feature detail.

## Known limitations

- Temporal validity: trained on 2008–2010 Portuguese retail-banking data;
  economic conditions and channel mix have moved on.
- Geographic specificity: one bank, one country.
- `unknown` category bias: six fields carry a nontrivial "declined to
  state" rate, which the model treats as a signal rather than imputing away.
- No fairness certification, despite age and marital status being model
  inputs.
